import React from 'react';

const CommentAuth = ({ lineDir = 'left' }) => {
  return (
    <div className='comment-auth'>
      <p>This is an auth module</p>
    </div>
  );
};

export default CommentAuth;
